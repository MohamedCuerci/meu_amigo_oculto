<%= form_with(model: user_preference) do |form| %>
  <% if user_preference.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_preference.errors.count, "error") %> prohibited this user_preference from being saved:</h2>

      <ul>
        <% user_preference.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nickname, style: "display: block" %>
    <%= form.text_field :nickname %>
  </div>

  <div>
    <%= form.label :age, style: "display: block" %>
    <%= form.text_field :age %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
